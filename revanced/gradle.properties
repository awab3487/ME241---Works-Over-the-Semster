org.gradle.parallel = true
org.gradle.caching = true
kotlin.code.style = official
# The extension is written in Java. Without this, AGP 9 adds the Kotlin standard library to its DEX file,
# which would then be merged into the patched app.
android.builtInKotlin = false
version = 1.0.0
